app:
  debug: false
  environment: "production"

server:
  reload: false
  workers: 4
  port: 8000

database:
  echo: false
  pool_size: 50
  max_overflow: 20

monitoring:
  logging:
    level: "INFO"
    format: "json"

